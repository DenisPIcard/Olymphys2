{% extends "base.html.twig" %}
{% block title %} Accueil Jury {% endblock %}



{% block navigation %}
<button type="button" class="btn btn-outline-primary btn-lg btn-bloc"  > <span class="fas fa-user">  </span>           
{{ jure.prenomJure|title }} {{ jure.nomJure|title }} 
  </button>
{{ include ('cyberjury/navigation.html.twig') }}
{#{{ knp_menu_render('jury', {'currentClass': 'active'}) }}#}

{% endblock %}
{% block titre3 %} Mes équipes {% endblock %}

{% block body %}
{% for equipe in listEquipes %}
    {% set lettre = equipe.lettre %}
    <p>
    <a     
        {% if (progression[equipe.lettre]) %}  class ="btn btn-outline-primary btn-lg"
        {% else %}class ="btn btn-primary btn-lg"
        {% endif %}
        href="{{ path('cyberjury_evaluer_une_equipe', {'id': equipe.id} ) }}" >{{ equipe.lettre }}</a>
    {{ equipe.titreProjet }}
    <a href="#" class="fas fa-clock" title="{{equipe.heure}} {{equipe.salle}}"> </a>
{#    {% for key,mem in memoires %}
        {%if key == lettre %}          
            <a href="https://olymphys.fr/public/upload/equipes/memoires/nat/{{ mem.memoire }}" class="fas fa-book-reader" title="Accéder au mémoire de l'équipe" target="_blank"> </a>
        {% endif %} 
    {% endfor %}
    #}
    <a href="{{ path('cyberjury_infos_equipe', {'id': equipe.id} ) }}" class="fas fa-info-circle" title="En savoir plus sur cette équipe"> </a>
    <a href="{{ path('cyberjury_phrases_amusantes', {'id': equipe.id} ) }}" class="fas fa-pencil-alt" title="Les phrases amusantes" > </a>
    <p />

{% endfor %}
{% endblock %}